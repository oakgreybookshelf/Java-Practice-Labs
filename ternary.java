// ternary operator ex:

class GFG {
    public static void main (String[] args) {
        int a = 10, b = 20;
        int res = (a > b) ? a : b; // ? is the ternary operator
        System.out.println(res);
    }
}
